# Cache Stampede 예제 실행 방법

본 문서는 Cache Stampede 예제 프로젝트의 실행 방법과 테스트 시나리오를 설명합니다.

## 1. 환경 설정

### 1.1 Redis 설치 및 실행
Cache Stampede 예제는 Redis를 캐시 저장소로 사용합니다. 다음 방법 중 하나로 Redis를 실행할 수 있습니다.

#### Docker를 이용한 Redis 실행 (권장)
```bash
docker run --name redis -p 6379:6379 -d redis
```

#### 로컬 Redis 서버 설치
- macOS: `brew install redis` 후 `brew services start redis`
- Ubuntu: `apt-get install redis-server` 후 `service redis-server start`

### 1.2 프로젝트 빌드 및 실행
```bash
cd /Users/haeseoky/work/study/cache
./gradlew bootRun
```

## 2. API 테스트

### 2.1 기본 API 테스트
각 캐시 전략별로 상품 조회 API를 테스트해 볼 수 있습니다.

#### Mutex Lock 방식
```bash
curl http://localhost:8080/api/products/mutex/1
```

#### 캐시 워밍 방식
```bash
curl http://localhost:8080/api/products/warming/1
```

#### PER 알고리즘 방식
```bash
curl http://localhost:8080/api/products/per/1
```

#### Stale-While-Revalidate 방식
```bash
curl http://localhost:8080/api/products/swr/1
```

### 2.2 Cache Stampede 시뮬레이션
동시에 여러 요청을 보내서 Cache Stampede 상황을 시뮬레이션하고 각 전략별 성능을 비교할 수 있습니다.

#### Mutex Lock 방식 시뮬레이션 (20개 동시 요청)
```bash
curl "http://localhost:8080/api/products/simulate?productId=1&strategy=mutex&concurrentRequests=20"
```

#### 캐시 워밍 방식 시뮬레이션 (20개 동시 요청)
```bash
curl "http://localhost:8080/api/products/simulate?productId=1&strategy=warming&concurrentRequests=20"
```

#### PER 알고리즘 방식 시뮬레이션 (20개 동시 요청)
```bash
curl "http://localhost:8080/api/products/simulate?productId=1&strategy=per&concurrentRequests=20"
```

#### Stale-While-Revalidate 방식 시뮬레이션 (20개 동시 요청)
```bash
curl "http://localhost:8080/api/products/simulate?productId=1&strategy=swr&concurrentRequests=20"
```

## 3. 캐시 전략별 특징 및 결과 분석

### 3.1 Mutex Lock 방식
- **특징**: 첫 번째 요청만 DB를 조회하고, 나머지 요청은 락을 획득할 때까지 대기
- **로그 관찰**: 여러 요청 중 하나만 "DB에서 상품 조회 시작" 로그가 출력되고, 나머지는 대기 후 캐시에서 조회
- **장점**: DB 부하 최소화, 일관된 데이터 제공
- **단점**: 첫 번째 요청이 오래 걸릴 경우 전체 응답 시간 증가

### 3.2 캐시 워밍 방식
- **특징**: 스케줄러를 통해 미리 인기 상품의 캐시를 갱신
- **로그 관찰**: "인기 상품 캐시 워밍 작업" 로그가 주기적으로 출력되는지 확인
- **장점**: 캐시 미스 최소화, 사용자 요청 시 지연 없음
- **단점**: 불필요한 데이터도 캐싱할 가능성

### 3.3 PER 알고리즘 방식
- **특징**: 캐시 만료 시간이 가까워질수록 확률적으로 미리 갱신
- **로그 관찰**: "남은 TTL 비율", "갱신 확률" 등의 로그를 통해 알고리즘 동작 확인
- **장점**: 캐시 갱신 부하 분산, 효율적인 리소스 사용
- **단점**: 구현 복잡성, 일부 요청은 여전히 캐시 미스 발생 가능

### 3.4 Stale-While-Revalidate 방식
- **특징**: 오래된 데이터를 반환하면서 백그라운드에서 갱신
- **로그 관찰**: "오래된 캐시 히트, 백그라운드 갱신 시작" 로그 확인
- **장점**: 항상 빠른 응답 시간, 백그라운드 갱신으로 DB 부하 분산
- **단점**: 약간 오래된 데이터 제공 가능성

## 4. 문제 해결 및 팁

### 4.1 Redis 연결 오류
- Redis가 실행 중인지 확인: `docker ps` 또는 `redis-cli ping`
- 연결 설정 확인: `application.properties`의 host와 port 확인

### 4.2 로그 레벨 조정
로그 레벨을 변경하여 더 자세한 정보를 확인할 수 있습니다.
```properties
# application.properties에 추가
logging.level.com.example.cache=TRACE
```

### 4.3 캐시 수동 삭제
테스트를 위해 Redis 캐시를 수동으로 삭제할 수 있습니다.
```bash
redis-cli
> KEYS *product*
> DEL product:1
```

## 5. 참고 자료

- [Redis 공식 문서](https://redis.io/documentation)
- [Spring Data Redis 문서](https://docs.spring.io/spring-data/redis/docs/current/reference/html/)
- [Cache Stampede 위키피디아](https://en.wikipedia.org/wiki/Cache_stampede)
